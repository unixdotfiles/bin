from setuptools import setup, find_packages

setup(name='wnmap',
      author='Eitan Adler',
      author_email='lists@eitanadler.com',
      scripts=['wnmap.py'],
      license='MIT',
      packages=find_packages()
      )
