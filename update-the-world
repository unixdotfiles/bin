while true; do sudo -n true; sleep 60; kill -0 "$$" || exit; done 2>/dev/null &

sudo -H port upgrade outdated
sudo -H gem update
sudo -H npm -g update
sudo -H pip install -U $(pip list --outdated | awk '!/Could not|ignored/ {print $1}')
